var bmi = function(weight,height) { return weight/(height*height);};

var weight=parseFloat(prompt("enter weight in kilograms"));

var height = parseFloat(prompt("enter height in meter"));

var result = bmi(weight,height);

alert(result);

//add, subtract, multiply, and divide.
